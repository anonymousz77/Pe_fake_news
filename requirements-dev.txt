# Pe_Fake_News_Dec — development dependencies
#
# Install on top of requirements.txt:
#     pip install -r requirements.txt -r requirements-dev.txt
#
# Pins were captured from `pip freeze` in the project venv (.venv,
# CPython 3.11.9) on 2026-09-01.

-r requirements.txt

# Installed during this session; pin taken from that run's `pip freeze`.
pytest==9.1.1

# UNPINNED — not installed in this venv, so there was no version to record
# and none has been invented. Pin it from `pip freeze` after installing.
ruff

# --- notebooks ---
jupytext==1.19.5
nbformat==5.11.1
